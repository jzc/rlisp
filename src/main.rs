#![allow(dead_code)]

mod sexpr;
mod scanner;
mod parser;
mod interpreter;

struct A {
    vec: Vec<String>
}

fn main() {
    // let vec = vec!["abc".to_string(), "def".to_string()];
    // let r = & A { vec };
    // r.vec[0];
    
}